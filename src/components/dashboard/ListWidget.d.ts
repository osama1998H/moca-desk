interface ListWidgetProps {
    dashboardName: string;
    widgetIdx: number;
    config: Record<string, unknown>;
}
export declare function ListWidget({ dashboardName, widgetIdx, config, }: ListWidgetProps): import("react/jsx-runtime").JSX.Element;
export {};
