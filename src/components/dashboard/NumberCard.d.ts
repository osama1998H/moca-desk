interface NumberCardProps {
    dashboardName: string;
    widgetIdx: number;
    config: Record<string, unknown>;
}
export declare function NumberCard({ dashboardName, widgetIdx, config, }: NumberCardProps): import("react/jsx-runtime").JSX.Element;
export {};
